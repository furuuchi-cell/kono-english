# KONO式英単語 - プロジェクト概要

英語単語学習アプリ。管理者（先生）と生徒の2ロールで構成される。

## 技術スタック

- **フロントエンド**: React + TypeScript (Create React App)
- **バックエンド**: Firebase Auth / Firestore / Hosting
- **Firebase プロジェクト**: `kono-english`
- **本番URL**: https://kono-english.web.app
- **デプロイコマンド**: `npm run build && firebase deploy --only hosting`

## ディレクトリ構成

```
src/
├── App.tsx                        # ルーティング定義
├── firebase.ts                    # Firebase初期化
├── types/index.ts                 # 共通型定義
├── contexts/AuthContext.tsx       # 認証コンテキスト（currentUser, userProfile）
├── hooks/
│   ├── useWords.ts                # クラス別単語リスト取得（キャッシュ付き）
│   └── useStudySession.ts        # 学習セッション管理
├── components/
│   ├── admin/
│   │   ├── AdminDashboard.tsx     # 管理者トップ（クラス一覧・作成・参加）
│   │   ├── ClassManagePage.tsx    # クラス詳細（範囲・生徒・テスト・クイズ）
│   │   ├── ClassSetupPage.tsx     # AS2EN001クラス初期化ページ
│   │   ├── WordEditPage.tsx       # 単語上書き編集（クラスごと）
│   │   ├── StudentProgressPage.tsx
│   │   ├── TestResultsPage.tsx
│   │   └── SessionResultsPage.tsx
│   ├── student/
│   │   ├── HomePage.tsx           # 生徒トップ（クラスカード・進捗・ランキング）
│   │   ├── StudyPage.tsx          # スピード周回（スワイプカード学習）
│   │   ├── WeeklyPlanPage.tsx     # 週間プラン
│   │   ├── WordListPage.tsx       # 全単語一覧
│   │   ├── TestPage.tsx           # チェックテスト
│   │   ├── TestWeekPage.tsx       # 週別テスト選択
│   │   ├── ClassroomQuiz.tsx      # 授業クイズ（リアルタイム）
│   │   ├── StudentLayout.tsx      # 授業クイズ監視ラッパー
│   │   ├── QuizHistoryPage.tsx
│   │   └── JoinClassPage.tsx
│   └── common/
│       ├── Header.tsx
│       ├── ProtectedRoute.tsx     # 認証ガード（グローバルメンテナンスチェックなし）
│       ├── WordListView.tsx       # 単語一覧表示コンポーネント
│       └── Tutorial.tsx / StudyTutorial.tsx
├── data/
│   ├── words.json                 # デフォルト単語セット
│   └── classWords/
│       └── AS2EN001.json          # AS2EN001専用単語（2351語、バンドル済み）
└── utils/
    ├── pos.ts                     # 品詞マッピング
    ├── rank.ts                    # ランク計算
    └── speech.ts                  # 音声読み上げ
```

## Firestore データ構造

```
classes/{classId}
  name: string                  # クラス表示名（例: "AS2EN001", "S1"）
  adminId: string               # オーナー管理者のUID
  coAdminIds: string[]          # 共同管理者のUID一覧
  studentIds: string[]          # 生徒のUID一覧
  wordSetId?: string            # 単語セット識別子（例: "AS2EN001"）
  maintenanceEnabled?: boolean  # クラス別メンテナンスモード
  createdAt: Timestamp

  /ranges/{rangeId}
    classId, weekLabel, startId, endId, lessonDate

  /tests/{testId}
    classId, title, startId, endId, isActive, isAutoGenerated, sortKey, createdAt

  /sessions/{sessionId}          # 授業クイズセッション
    wordIds, intervalSeconds, startedAt, status, createdBy, rangeId
    /answers/{uid}

  /settings/wordOverrides        # 単語上書き（任意）
    {wordId: {japanese, pos, example, mnemonic, pronunciation}}

users/{uid}
  displayName, furigana, email, role, classIds, createdAt

  /progress/{classId}
    {wordId: {wordId, status, consecutiveCorrect, lastStudied}}

  /dismissedSessions/{sessionId}
```

## 重要な設計判断

### クラスIDとクラス名の分離
- クラスIDは Firestore の自動生成ID（ランダム）を使用
- クラス名（`name`）と単語セット識別子（`wordSetId`）は別フィールド
- セキュリティ上、クラスIDが予測可能にならないようにしている

### クラス別単語セット（useWords.ts）
- `wordSetId` で使用する単語セットを判別
- `AS2EN001` → `src/data/classWords/AS2EN001.json`（バンドル済み、2351語）
- デフォルト → `src/data/words.json`
- Firestoreの `wordOverrides` で上書き可能
- `TOTAL_WORDS_BY_SET`: クラス別総単語数（AS2EN001: 2351）
- `WORDS_PER_PAGE_BY_SET`: クラス別1ページ単語数（AS2EN001: 150）
- `getWordsSync(classId)`: 同期取得（StudyPage等で使用）
- `invalidateWordsCache(classId)`: 編集後にキャッシュクリア

### AS2EN001クラスの構成
- 2351語（IPA発音記号付き、[自]/[他]動詞マーカー付き）
- 難易度順ソート: 簡単(91語) → ラベルなし(2102語) → ニッチ(67語) → 難しい(91語)
- 週150語: 月〜金30語ずつ + 土曜150語レビュー = 16週96範囲
- 初期化は `/admin/setup/AS2EN001` ページから実行

### 管理者権限
- `adminId`: クラスオーナー（作成者）
- `coAdminIds`: 共同管理者。AdminDashboardの「クラスに参加」で追加
- 共同管理者はクラス管理ページにフルアクセス可能
- 管理者のFirestore（Storage等）直接アクセスは Firebase Console から

### メンテナンスモード（クラス別）
- `classes/{classId}.maintenanceEnabled: true` でそのクラスをメンテ状態に
- ClassManagePage のヘッダーからトグル操作
- 生徒のHomePage でメンテ中クラスは「メンテナンス中」バッジ表示＆コンテンツ非表示
- ※グローバルメンテナンス（settings/maintenance）は廃止済み

### ランキング表示条件
- 生徒が2人以上いるクラスのみ習得ランキング・クイズランキングを表示
- 1人以下の場合はランキングセクション自体を非表示

### 進捗管理
- 進捗は `users/{uid}/progress/{classId}` に単語IDをキーとして保存
- status: `'unlearned' | 'review' | 'mastered'`
- 連続正解数 `consecutiveCorrect` で mastered 判定

### 授業クイズ（リアルタイム）
- セッション開始: 管理者がClassManagePageの「授業クイズ」タブから実行
- StudentLayoutが全クラスのセッションをonSnapshotで監視
- セッション終了後、生徒はdismissedSessionsに記録して非表示化

## ルーティング（App.tsx）

| パス | コンポーネント | ロール |
|------|---------------|--------|
| /home | HomePage | student |
| /study/:classId/:rangeId | StudyPage | student |
| /wordlist/:classId | WordListPage | student |
| /test/:classId/:testId | TestPage | student |
| /weekly-plan/:classId/:rangeId | WeeklyPlanPage | student |
| /test-week/:classId/:rangeId | TestWeekPage | student |
| /quiz-history/:classId | QuizHistoryPage | student |
| /join-class | JoinClassPage | student |
| /admin | AdminDashboard | admin |
| /admin/class/:classId | ClassManagePage | admin |
| /admin/class/:classId/words | WordEditPage | admin |
| /admin/class/:classId/student/:studentId | StudentProgressPage | admin |
| /admin/class/:classId/test/:testId | TestResultsPage | admin |
| /admin/class/:classId/session/:sessionId | SessionResultsPage | admin |
| /admin/setup/AS2EN001 | ClassSetupPage | admin |

## 開発メモ

- スタイルは全てインラインスタイル（CSS-in-JS形式、Tailwindなし）
- ESLintの警告（unused vars, missing deps）は既存のもので、修正不要
- `npm run build` でビルド、警告はあってもエラーがなければOK
- デプロイは毎回 `npm run build && firebase deploy --only hosting`
